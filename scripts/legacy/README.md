# 历史验证脚本（legacy）

项目早期（8 月 4 日）的探路/可行性验证脚本，**已被 `src/` 中的正式实现替代，仅作历史参考，不再维护**。如需复现验证思路，可参考下表对应的正式实现。

| 脚本 | 日期 | 验证内容 | 正式替代 |
|------|------|----------|----------|
| `extract_test.py` | 08-04 | PDF 下载 + 文本提取质量检测 | `src/data/extract.py` |
| `kg_test.py` | 08-04 | 迷你知识图谱构建 + 查询测试 | `src/data/kg_build.py` |
| `extract_content.py` | 08-04 | PDF 正文页深度质量检查 | `src/data/extract.py` |

> 这些脚本由 Day 1 / Day 2 spike 验证产生，验证通过后其能力在 `src/` 中重新实现并接入测试。删除本目录不影响任何功能，git 历史中保留完整版本。
